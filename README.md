# Hello-DevOps---Project-1
Project 1: “Hello DevOps” CI/CD Pipeline

Create a simple “Hello World” web app (Node.js or Python Flask).

Push the code to GitHub.

Configure a Jenkins pipeline:

Checkout code from GitHub.

Run basic unit tests.

Build and package (zip or tar).

Archive artifacts.

Configure webhook-based trigger (GitHub → Jenkins).

